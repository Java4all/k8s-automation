---
apiVersion: kubeadm.k8s.io/v1beta3
kind: InitConfiguration
localAPIEndpoint:
  advertiseAddress: {{ hostvars[groups['masters'][0]]['ansible_default_ipv4']['address'] }}
  bindPort: 6443

---
apiVersion: kubeadm.k8s.io/v1beta3
kind: ClusterConfiguration
kubernetesVersion: v{{ cluster.k8s_version }}
controlPlaneEndpoint: {{ cluster.control_plane.endpoint_vip }}:{{ cluster.control_plane.endpoint_port }}
networking:
  serviceSubnet: {{ cluster.networking.service_cidr | default('10.0.0.0/24') }}
  podSubnet: {{ cluster.networking.pod_cidr }}
  dnsDomain: cluster.local
apiServer:
  extraArgs:
    proxy-client-key-file: /etc/kubernetes/pki/front-proxy-client.key
    proxy-client-cert-file: /etc/kubernetes/pki/front-proxy-client.crt
controllerManager:
  extraArgs:
    bind-address: 0.0.0.0
scheduler:
  extraArgs:
    bind-address: 0.0.0.0
etcd:
  local:
    dataDir: /var/lib/etcd
certificatesDir: /etc/kubernetes/pki
imageRepository: registry.k8s.io

---
apiVersion: kubeproxy.config.k8s.io/v1alpha1
kind: KubeProxyConfiguration
mode: iptables
