---
- name: Install Containerd
  block:
    - name: Update package cache
      apt:
        update_cache: yes
        cache_valid_time: 3600

    - name: Install containerd
      apt:
        name: containerd
        state: present

- name: Configure Containerd
  block:
    - name: Create containerd config directory
      file:
        path: /etc/containerd
        state: directory
        mode: '0755'

    - name: Generate default containerd config
      command: containerd config default
      register: containerd_default_config
      changed_when: false
      when: not ansible_check_mode

    - name: Save containerd config
      copy:
        content: "{{ containerd_default_config.stdout }}"
        dest: /etc/containerd/config.toml
        owner: root
        group: root
        mode: '0644'
        backup: yes
      when: not ansible_check_mode

    - name: Enable systemd cgroup in containerd config
      ansible.builtin.replace:
        path: /etc/containerd/config.toml
        regexp: 'SystemdCgroup\s*=\s*false'
        replace: 'SystemdCgroup = true'
        backup: yes
      when: not ansible_check_mode

- name: Configure Containerd proxy settings
  block:
    - name: Create containerd service.d directory
      file:
        path: /etc/systemd/system/containerd.service.d
        state: directory
        mode: '0755'

    - name: Create containerd proxy config
      template:
        src: containerd-proxy.conf.j2
        dest: /etc/systemd/system/containerd.service.d/http-proxy.conf
        owner: root
        group: root
        mode: '0644'
      when: cluster.proxy.enabled and not ansible_check_mode
      notify: restart containerd
  when: not ansible_check_mode

- name: Reload systemd and restart containerd
  block:
    - name: Reload systemd daemon
      systemd:
        daemon_reload: yes

    - name: Enable containerd service
      systemd:
        name: containerd
        enabled: yes

    - name: Restart containerd
      systemd:
        name: containerd
        state: restarted

    - name: Verify containerd is running
      systemd:
        name: containerd
      register: containerd_status

    - name: Display containerd status
      debug:
        msg: "Containerd status: {{ containerd_status.status.ActiveState | default('Not running in check mode') }}"
  when: not ansible_check_mode
